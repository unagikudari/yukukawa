# Kawa Phase 0 — local configuration example. Copy to .env (git-ignored) and adjust.
# The repository ships placeholders only; never commit a real .env.

# PostgreSQL DSN for the local kawa database (libpq / psycopg format).
# Default assumes a local `kawa` database reachable over the unix socket (peer auth).
KAWA_DSN=dbname=kawa

# Domain identity this checkout writes Events as (synthetic placeholders).
KAWA_ORIGIN_NODE=node-a
KAWA_ACTOR_REF=vendor-local

# GoatCounter collector (scripts/collect_goatcounter.py) — placeholders only.
# GOATCOUNTER_SINCE = first day the counter existed; earlier days are refused
# (the API reports 0 for pre-instrument days; that is absence, not a measurement).
GOATCOUNTER_SITE=https://example.goatcounter.com
GOATCOUNTER_API_TOKEN=replace-me
GOATCOUNTER_SINCE=2026-01-01

# Node identity for systemd-run collectors (units may pin their own value).
KAWA_NODE=node-a

# Reachability probe collector (scripts/collect_node_probe.py) — the target
# allowlist is a git-ignored OPERATOR file, default ~/.kawa/probe_targets.conf,
# of `label = "url"` pairs (#189 §D). Placeholder shape only — real URLs never
# enter the repo; the label is the only part that reaches the projection/UI:
#   node-example = "https://node-example.invalid:8099/"
# NOTE: kawa-probe.service does not read .env — the env var below only affects
# manual runs; the systemd unit uses the default path (or its own Environment=).
#KAWA_PROBE_TARGETS=~/.kawa/probe_targets.conf
